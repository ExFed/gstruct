rootProject.name = 'gStruct'

include 'lang'
include 'test-util'
include 'util'

// define subproject directories
rootProject.children.each { project ->
    project.projectDir = file("subprojects/$project.name")
}
